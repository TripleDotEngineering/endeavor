<?ev for key in ref._used.keys() ?>
<?ev set r = data['references'][key] ?>
<?ev set year = r.get('issued', {}).get('date-parts', [[0]])[0][0] ?>
\bibitem[${{ authorRef }}(${{ year }})]{${{key}}}
    <?ev for author in r['author'] ?>${{ author['given'] }} ${{ author['family'] }}${{ ", " if not loop.last else "" }}<?ev endfor ?>
    ${{ year }}. \emph{${{ r['title'] | replace(" &"," \&") }}}. <?ev if r['publication']?>${{ r['publication'] }}.<?ev endif?><?ev if r['url'] ?>\url{${{ r['url'] }}}<?ev endif ?>
<?ev endfor ?>
